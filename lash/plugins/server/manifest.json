{
  "name": "server",
  "category": "Server Tools",
  "description": "Remote command injection and auto-discovery daemon",
  "commands": {
    "injection": {
      "module": "lash.plugins.server.cli:injection",
      "description": "Remote command injection — active server / passive client",
      "requires": ["rich>=12.6.0"]
    },
    "seeker": {
      "module": "lash.plugins.server.cli:seeker",
      "description": "Background daemon that auto-discovers and connects to Lash servers",
      "requires": ["rich>=12.6.0"]
    }
  }
}
